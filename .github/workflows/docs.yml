name: Docs

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

# Serialize docs deploys; both the push-to-main and v-tag triggers fire on
# the same bump commit, and both push to gh-pages — without this, one wins
# and the other fails with "fetch first".
concurrency:
  group: docs-deploy
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "pyproject.toml"

      - name: Set up Python
        run: uv python install 3.14

      - name: Install dependencies
        run: uv sync --group docs

      - name: Deploy docs (version tag)
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          uv run mike deploy --push --update-aliases $VERSION latest
          uv run mike set-default --push latest

      - name: Deploy docs (main branch)
        if: github.ref == 'refs/heads/main'
        run: |
          uv run mike deploy --push dev
          uv run mike set-default --push latest
