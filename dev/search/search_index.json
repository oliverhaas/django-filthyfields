{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Django Filthy Fields","text":"<p>Tracking dirty fields on a Django model instance.</p> <p>Dirty means that field in-memory and database values are different.</p> <p>Fork of django-dirtyfields</p> <p>This started as a fork of django-dirtyfields with a rewritten \"lazy\" descriptor-based internal implementation, and has since diverged with its own feature set and release cadence.</p>"},{"location":"#why-this-fork","title":"Why This Fork?","text":"<p>The original django-dirtyfields captures model state by making a full snapshot of the model on instance initialization. This means every field value is copied every time a model instance is initialized, regardless of whether you'll modify the instance.</p> <p>This fork uses lazy descriptor-based tracking, which for typical use-cases where one reads/writes fields at most once each has less overhead.</p>"},{"location":"#benchmark-results","title":"Benchmark Results","text":"<p>Python 3.14, 10,000 instances \u00d7 20 fields, 5 iterations. Numbers in parentheses are overhead vs. plain Django.</p> Scenario Plain filthyfields dirtyfields <code>.only(1 field)</code> + read 1 field 38 ms 42 ms (+4) 140 ms (+101) Load 20 fields + read 20 fields 140 ms 162 ms (+22) 516 ms (+376) <code>.only(1 field)</code> + write 1 field 38 ms 47 ms (+9) 141 ms (+103) Load 20 fields + write 20 fields 133 ms 244 ms (+111) 518 ms (+384) <code>.only(1 field)</code> + read+write 1 field 38 ms 48 ms (+10) 141 ms (+103) Load 20 fields + read+write 20 fields 138 ms 254 ms (+116) 527 ms (+390) <p>Across the suite, filthyfields overhead is 3\u00d7\u201328\u00d7 smaller than dirtyfields overhead \u2014 biggest on read-only paths where the Cython-backed descriptor avoids the full-model snapshot that upstream does on every load.</p> <p>Run the benchmark yourself: <code>uv run pytest tests/test_benchmark.py -m benchmark -s</code></p>"},{"location":"#compatibility","title":"Compatibility","text":"Django Python 6.0 3.14, 3.14t"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>from django.db import models\nfrom dirtyfields import DirtyFieldsMixin\n\nclass MyModel(DirtyFieldsMixin, models.Model):\n    name = models.CharField(max_length=100)\n    count = models.IntegerField(default=0)\n\n# Create and save a model\nobj = MyModel.objects.create(name=\"test\", count=5)\nobj.is_dirty()  # False\n\n# Modify it\nobj.name = \"changed\"\nobj.is_dirty()  # True\nobj.get_dirty_fields()  # {'name': 'test'}\n</code></pre>"},{"location":"#getting-help","title":"Getting Help","text":"<ul> <li>GitHub Discussions - Questions and community support</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.14+</li> <li>Django 6.0+</li> </ul>"},{"location":"getting-started/installation/#install-with-pip","title":"Install with pip","text":"<pre><code>pip install django-filthyfields\n</code></pre>"},{"location":"getting-started/installation/#install-with-uv","title":"Install with uv","text":"<pre><code>uv add django-filthyfields\n</code></pre>"},{"location":"getting-started/installation/#verify-installation","title":"Verify Installation","text":"<pre><code>&gt;&gt;&gt; from dirtyfields import DirtyFieldsMixin\n&gt;&gt;&gt; DirtyFieldsMixin\n&lt;class 'dirtyfields.dirtyfields.DirtyFieldsMixin'&gt;\n</code></pre> <p>Import Path</p> <p>The package is installed as <code>django-filthyfields</code> but you import from <code>dirtyfields</code>. This maintains compatibility with the original django-dirtyfields package.</p>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":""},{"location":"getting-started/quickstart/#add-the-mixin-to-your-model","title":"Add the Mixin to Your Model","text":"<p>To track dirty fields on a model, inherit from <code>DirtyFieldsMixin</code>:</p> <pre><code>from django.db import models\nfrom dirtyfields import DirtyFieldsMixin\n\nclass ExampleModel(DirtyFieldsMixin, models.Model):\n    boolean = models.BooleanField(default=True)\n    characters = models.CharField(blank=True, max_length=80)\n</code></pre> <p>Mixin Order</p> <p><code>DirtyFieldsMixin</code> should come before <code>models.Model</code> in the inheritance list.</p>"},{"location":"getting-started/quickstart/#check-if-a-model-is-dirty","title":"Check if a Model is Dirty","text":"<p>Use <code>is_dirty()</code> to check if any fields have been modified:</p> <pre><code>&gt;&gt;&gt; model = ExampleModel.objects.create(boolean=True, characters=\"first value\")\n&gt;&gt;&gt; model.is_dirty()\nFalse\n\n&gt;&gt;&gt; model.boolean = False\n&gt;&gt;&gt; model.is_dirty()\nTrue\n</code></pre>"},{"location":"getting-started/quickstart/#get-dirty-fields","title":"Get Dirty Fields","text":"<p>Use <code>get_dirty_fields()</code> to get a dictionary of changed fields with their original values:</p> <pre><code>&gt;&gt;&gt; model = ExampleModel.objects.create(boolean=True, characters=\"first value\")\n&gt;&gt;&gt; model.boolean = False\n&gt;&gt;&gt; model.characters = \"second value\"\n\n&gt;&gt;&gt; model.get_dirty_fields()\n{'boolean': True, 'characters': 'first value'}\n</code></pre> <p>The returned dictionary maps field names to their original (database) values, not the new values.</p>"},{"location":"getting-started/quickstart/#save-and-reset","title":"Save and Reset","text":"<p>After saving, the dirty state is cleared:</p> <pre><code>&gt;&gt;&gt; model.is_dirty()\nTrue\n&gt;&gt;&gt; model.save()\n&gt;&gt;&gt; model.is_dirty()\nFalse\n&gt;&gt;&gt; model.get_dirty_fields()\n{}\n</code></pre>"},{"location":"guide/advanced/","title":"Advanced Features","text":""},{"location":"guide/advanced/#limiting-tracked-fields","title":"Limiting Tracked Fields","text":""},{"location":"guide/advanced/#including-specific-fields-whitelist","title":"Including Specific Fields (Whitelist)","text":"<p>Use <code>FIELDS_TO_CHECK</code> to only track specific fields:</p> <pre><code>class MyModel(DirtyFieldsMixin, models.Model):\n    FIELDS_TO_CHECK = ['important_field']\n\n    important_field = models.CharField(max_length=100)\n    unimportant_field = models.CharField(max_length=100)\n\n&gt;&gt;&gt; obj.important_field = \"changed\"\n&gt;&gt;&gt; obj.is_dirty()\nTrue\n\n&gt;&gt;&gt; obj2 = MyModel.objects.get(pk=2)\n&gt;&gt;&gt; obj2.unimportant_field = \"changed\"\n&gt;&gt;&gt; obj2.is_dirty()\nFalse  # Not tracked!\n</code></pre> <p>This is useful when you only care about changes to specific fields, or want to improve performance by not tracking large fields.</p>"},{"location":"guide/advanced/#excluding-specific-fields-blacklist","title":"Excluding Specific Fields (Blacklist)","text":"<p>Use <code>FIELDS_TO_CHECK_EXCLUDE</code> to track all fields except the specified ones:</p> <pre><code>class MyModel(DirtyFieldsMixin, models.Model):\n    FIELDS_TO_CHECK_EXCLUDE = ['updated_at', 'last_login']\n\n    name = models.CharField(max_length=100)\n    email = models.EmailField()\n    updated_at = models.DateTimeField(auto_now=True)\n    last_login = models.DateTimeField(null=True)\n\n&gt;&gt;&gt; obj.name = \"changed\"\n&gt;&gt;&gt; obj.is_dirty()\nTrue  # 'name' is tracked\n\n&gt;&gt;&gt; obj2 = MyModel.objects.get(pk=2)\n&gt;&gt;&gt; obj2.updated_at = timezone.now()\n&gt;&gt;&gt; obj2.is_dirty()\nFalse  # 'updated_at' is excluded!\n</code></pre> <p>This is more convenient than <code>FIELDS_TO_CHECK</code> when you want to track most fields but exclude a few (e.g., auto-updated timestamps).</p> <p>Cannot Use Both</p> <p>You cannot use both <code>FIELDS_TO_CHECK</code> and <code>FIELDS_TO_CHECK_EXCLUDE</code> on the same model. Attempting to do so will raise a <code>ValueError</code>.</p>"},{"location":"guide/advanced/#model-inheritance","title":"Model Inheritance","text":""},{"location":"guide/advanced/#abstract-base-classes","title":"Abstract Base Classes","text":"<p>Dirty field tracking works with abstract base classes:</p> <pre><code>class BaseModel(DirtyFieldsMixin, models.Model):\n    created = models.DateTimeField(auto_now_add=True)\n\n    class Meta:\n        abstract = True\n\nclass Article(BaseModel):\n    title = models.CharField(max_length=100)\n\n&gt;&gt;&gt; article.title = \"new title\"\n&gt;&gt;&gt; article.is_dirty()\nTrue\n</code></pre>"},{"location":"guide/advanced/#proxy-models","title":"Proxy Models","text":"<p>Proxy models inherit dirty field tracking from their parent:</p> <pre><code>class Article(DirtyFieldsMixin, models.Model):\n    title = models.CharField(max_length=100)\n    is_published = models.BooleanField(default=False)\n\nclass PublishedArticle(Article):\n    class Meta:\n        proxy = True\n\n&gt;&gt;&gt; published = PublishedArticle.objects.get(pk=1)\n&gt;&gt;&gt; published.title = \"changed\"\n&gt;&gt;&gt; published.is_dirty()\nTrue\n</code></pre>"},{"location":"guide/advanced/#performance-considerations","title":"Performance Considerations","text":""},{"location":"guide/advanced/#descriptor-based-tracking","title":"Descriptor-Based Tracking","text":"<p>Unlike the original django-dirtyfields which uses signals to capture model state on every load, this implementation uses descriptors that only store values when fields are modified.</p> <p>This means:</p> <ul> <li>Loading models is faster: No state copying on <code>post_init</code></li> <li>Memory usage is lower: Only changed fields are stored</li> <li>Saving models is simpler: No <code>post_save</code> signal handler</li> </ul>"},{"location":"guide/advanced/#when-to-use-save_dirty_fields","title":"When to Use <code>save_dirty_fields()</code>","text":"<p>Use <code>save_dirty_fields()</code> instead of <code>save()</code> when:</p> <ul> <li>You've only modified a few fields on a model with many fields</li> <li>You want to minimize database write load</li> <li>You're in a tight loop updating many objects</li> </ul> <pre><code># Instead of this:\nobj.status = 'completed'\nobj.save()  # Updates all fields\n\n# Do this:\nobj.status = 'completed'\nobj.save_dirty_fields()  # Only updates 'status'\n</code></pre>"},{"location":"guide/advanced/#bulk-operations","title":"Bulk Operations","text":"<p>Django's <code>bulk_update()</code> and <code>bulk_create()</code> bypass the model's <code>save()</code> method, so dirty tracking doesn't happen automatically. Use the helper functions to manually manage dirty state:</p> <pre><code>from dirtyfields import capture_dirty_state, reset_dirty_state\n\n# Modify multiple instances\ninstances = list(MyModel.objects.filter(status='pending'))\nfor obj in instances:\n    obj.status = 'processed'\n\n# Capture dirty state before bulk operation\ncapture_dirty_state(instances)\n\n# Perform bulk update (bypasses save())\nMyModel.objects.bulk_update(instances, ['status'])\n\n# Reset dirty state after bulk operation\nreset_dirty_state(instances)\n\n# Now instances are clean, but was_dirty() still works\nfor obj in instances:\n    print(f\"{obj.pk} was dirty: {obj.was_dirty()}\")\n</code></pre> <p>You can also reset only specific fields:</p> <pre><code># Only reset the 'status' field, keep other fields dirty\nreset_dirty_state(instances, fields=['status'])\n</code></pre>"},{"location":"guide/advanced/#transaction-limitations","title":"Transaction Limitations","text":"<p>Rollback Behavior</p> <p>If a transaction is rolled back, the in-memory model state will not be automatically restored. The model will appear \"clean\" even though the database still has the old values.</p> <pre><code>from django.db import transaction\n\nobj = MyModel.objects.get(pk=1)\nobj.name = \"new name\"\n\ntry:\n    with transaction.atomic():\n        obj.save()\n        raise Exception(\"Rollback!\")\nexcept:\n    pass\n\n# obj.is_dirty() is now False, but the database has the old value!\nobj.refresh_from_db()  # Use this to sync state\n</code></pre>"},{"location":"guide/advanced/#refreshing-from-database","title":"Refreshing from Database","text":"<p>Use <code>refresh_from_db()</code> to reset the dirty state and reload values from the database:</p> <pre><code>&gt;&gt;&gt; obj.name = \"new name\"\n&gt;&gt;&gt; obj.is_dirty()\nTrue\n&gt;&gt;&gt; obj.refresh_from_db()\n&gt;&gt;&gt; obj.is_dirty()\nFalse\n&gt;&gt;&gt; obj.name\n'old name'  # Restored from database\n</code></pre>"},{"location":"guide/advanced/#deferred-fields","title":"Deferred Fields","text":"<p>When using <code>.only()</code> or <code>.defer()</code>, only the loaded fields are tracked:</p> <pre><code>&gt;&gt;&gt; obj = MyModel.objects.only('name').get(pk=1)\n&gt;&gt;&gt; obj.name = \"changed\"\n&gt;&gt;&gt; obj.is_dirty()\nTrue\n&gt;&gt;&gt; obj.get_dirty_fields()\n{'name': 'old name'}\n\n# Accessing a deferred field loads it from the database\n&gt;&gt;&gt; obj.other_field  # Loads from DB\n&gt;&gt;&gt; obj.other_field = \"changed\"\n&gt;&gt;&gt; obj.get_dirty_fields()\n{'name': 'old name', 'other_field': 'original value'}\n</code></pre>"},{"location":"guide/advanced/#many-to-many-field-tracking","title":"Many-to-Many Field Tracking","text":"<p>M2M fields are not tracked by default because checking them requires additional database queries. To enable M2M tracking, set <code>ENABLE_M2M_CHECK = True</code>:</p> <pre><code>class Article(DirtyFieldsMixin, models.Model):\n    ENABLE_M2M_CHECK = True\n\n    title = models.CharField(max_length=100)\n    tags = models.ManyToManyField(Tag)\n</code></pre> <p>Then use <code>check_m2m=True</code> to include M2M fields in dirty checks:</p> <pre><code>&gt;&gt;&gt; article = Article.objects.get(pk=1)\n&gt;&gt;&gt; article.tags.all()\n&lt;QuerySet [&lt;Tag: python&gt;, &lt;Tag: django&gt;]&gt;\n\n# First check captures the original state\n&gt;&gt;&gt; article.is_dirty(check_m2m=True)\nFalse\n\n# Add a new tag\n&gt;&gt;&gt; article.tags.add(Tag.objects.get(pk=3))\n&gt;&gt;&gt; article.is_dirty(check_m2m=True)\nTrue\n\n&gt;&gt;&gt; article.get_dirty_fields(check_m2m=True)\n{'tags': {1, 2}}  # Original state before changes\n\n# After save, state is re-captured\n&gt;&gt;&gt; article.save()\n&gt;&gt;&gt; article.is_dirty(check_m2m=True)\nFalse\n\n# was_dirty works for M2M too\n&gt;&gt;&gt; article.was_dirty(check_m2m=True)\nTrue\n&gt;&gt;&gt; article.get_was_dirty_fields(check_m2m=True)\n{'tags': {1, 2}}  # What it was before save\n</code></pre> <p>Performance Impact</p> <p>M2M checking generates extra queries each time you check. The original M2M state is captured on the first <code>check_m2m=True</code> call and re-captured after each save. Only enable it when you specifically need to track M2M changes.</p>"},{"location":"guide/advanced/#custom-comparison-functions","title":"Custom Comparison Functions","text":"<p>The default comparison uses simple equality (<code>==</code>). For special cases like timezone-aware datetime comparisons, you can provide a custom comparison function:</p> <pre><code>from dirtyfields import DirtyFieldsMixin, timezone_support_compare\n\nclass MyModel(DirtyFieldsMixin, models.Model):\n    compare_function = (timezone_support_compare, {})\n\n    updated_at = models.DateTimeField()\n</code></pre> <p>The <code>compare_function</code> is a tuple of <code>(function, kwargs)</code>. The function receives <code>(new_value, old_value, **kwargs)</code> and returns <code>True</code> if values are equal.</p> <p>You can also write your own:</p> <pre><code>def case_insensitive_compare(new_value, old_value):\n    if isinstance(new_value, str) and isinstance(old_value, str):\n        return new_value.lower() == old_value.lower()\n    return new_value == old_value\n\nclass MyModel(DirtyFieldsMixin, models.Model):\n    compare_function = (case_insensitive_compare, {})\n</code></pre>"},{"location":"guide/advanced/#custom-normalisation-functions","title":"Custom Normalisation Functions","text":"<p>To transform values before they're returned by <code>get_dirty_fields()</code>, use a normalisation function:</p> <pre><code>from datetime import datetime\n\ndef normalise_for_json(value):\n    if isinstance(value, datetime):\n        return value.isoformat()\n    return value\n\nclass MyModel(DirtyFieldsMixin, models.Model):\n    normalise_function = (normalise_for_json, {})\n</code></pre> <p>This is useful when you need to serialize dirty field values, for example when logging changes to JSON.</p>"},{"location":"guide/basic-usage/","title":"Basic Usage","text":""},{"location":"guide/basic-usage/#core-methods","title":"Core Methods","text":""},{"location":"guide/basic-usage/#is_dirtycheck_relationshipfalse-check_m2mfalse","title":"<code>is_dirty(check_relationship=False, check_m2m=False)</code>","text":"<p>Returns <code>True</code> if any tracked fields have been modified since the model was loaded or last saved.</p> <pre><code>&gt;&gt;&gt; obj = MyModel.objects.get(pk=1)\n&gt;&gt;&gt; obj.is_dirty()\nFalse\n&gt;&gt;&gt; obj.name = \"new name\"\n&gt;&gt;&gt; obj.is_dirty()\nTrue\n</code></pre> <p>Parameters:</p> <ul> <li><code>check_relationship</code> (bool): If <code>True</code>, also checks foreign key relationships. Default: <code>False</code>.</li> <li><code>check_m2m</code> (bool): If <code>True</code>, also checks M2M relationships. Requires <code>ENABLE_M2M_CHECK=True</code>. Default: <code>False</code>.</li> </ul>"},{"location":"guide/basic-usage/#get_dirty_fieldscheck_relationshipfalse-check_m2mfalse-verbosefalse","title":"<code>get_dirty_fields(check_relationship=False, check_m2m=False, verbose=False)</code>","text":"<p>Returns a dictionary of dirty fields with their original values.</p> <pre><code>&gt;&gt;&gt; obj.name = \"new name\"\n&gt;&gt;&gt; obj.get_dirty_fields()\n{'name': 'old name'}\n</code></pre> <p>Parameters:</p> <ul> <li><code>check_relationship</code> (bool): If <code>True</code>, includes foreign key changes. Default: <code>False</code>.</li> <li><code>check_m2m</code> (bool): If <code>True</code>, includes M2M changes. Requires <code>ENABLE_M2M_CHECK=True</code>. Default: <code>False</code>.</li> <li><code>verbose</code> (bool): If <code>True</code>, returns both old and new values. Default: <code>False</code>.</li> </ul>"},{"location":"guide/basic-usage/#save_dirty_fields","title":"<code>save_dirty_fields()</code>","text":"<p>Saves only the fields that have been modified, rather than all fields.</p> <pre><code>&gt;&gt;&gt; obj.name = \"new name\"\n&gt;&gt;&gt; obj.save_dirty_fields()  # Only updates 'name' field\n</code></pre> <p>This can be more efficient than <code>save()</code> when you've only modified a few fields on a model with many fields.</p>"},{"location":"guide/basic-usage/#was_dirtycheck_relationshipfalse","title":"<code>was_dirty(check_relationship=False)</code>","text":"<p>Check if the instance was dirty before the last save. Useful in <code>post_save</code> signals or after saving to know what changed.</p> <pre><code>&gt;&gt;&gt; obj.name = \"new name\"\n&gt;&gt;&gt; obj.save()\n&gt;&gt;&gt; obj.is_dirty()\nFalse\n&gt;&gt;&gt; obj.was_dirty()\nTrue\n</code></pre>"},{"location":"guide/basic-usage/#get_was_dirty_fieldscheck_relationshipfalse","title":"<code>get_was_dirty_fields(check_relationship=False)</code>","text":"<p>Get the fields that were dirty before the last save.</p> <pre><code>&gt;&gt;&gt; obj.name = \"new name\"\n&gt;&gt;&gt; obj.save()\n&gt;&gt;&gt; obj.get_was_dirty_fields()\n{'name': 'old name'}\n</code></pre> <p>This is particularly useful in signal handlers:</p> <pre><code>from django.db.models.signals import post_save\n\ndef my_handler(sender, instance, **kwargs):\n    if instance.was_dirty():\n        changed = instance.get_was_dirty_fields()\n        if 'status' in changed:\n            # Status changed from changed['status'] to instance.status\n            notify_status_change(instance)\n\npost_save.connect(my_handler, sender=MyModel)\n</code></pre>"},{"location":"guide/basic-usage/#async-support","title":"Async Support","text":"<p><code>asave()</code> and <code>arefresh_from_db()</code> are supported and behave the same way as their sync counterparts:</p> <pre><code>await obj.asave()              # captures was_dirty, saves, then resets dirty state\nawait obj.arefresh_from_db()   # reloads from DB and resets dirty state\n</code></pre> <p>All check methods (<code>is_dirty()</code>, <code>get_dirty_fields()</code>, <code>was_dirty()</code>, <code>get_was_dirty_fields()</code>) are plain synchronous calls on the in-memory instance and are safe to call from async code.</p>"},{"location":"guide/basic-usage/#verbose-mode","title":"Verbose Mode","text":"<p>When <code>verbose=True</code>, <code>get_dirty_fields()</code> returns both old and new values:</p> <pre><code>&gt;&gt;&gt; obj.name = \"new name\"\n&gt;&gt;&gt; obj.get_dirty_fields(verbose=True)\n{'name': {'saved': 'old name', 'current': 'new name'}}\n</code></pre>"},{"location":"guide/basic-usage/#tracking-foreign-keys","title":"Tracking Foreign Keys","text":"<p>By default, foreign key changes are not tracked. Use <code>check_relationship=True</code> to include them:</p> <pre><code>class Article(DirtyFieldsMixin, models.Model):\n    title = models.CharField(max_length=100)\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)\n\n&gt;&gt;&gt; article.author = new_author\n&gt;&gt;&gt; article.is_dirty()  # False - FK not tracked by default\n&gt;&gt;&gt; article.is_dirty(check_relationship=True)  # True\n&gt;&gt;&gt; article.get_dirty_fields(check_relationship=True)\n{'author': &lt;Author: old_author&gt;}\n</code></pre>"},{"location":"guide/basic-usage/#file-fields","title":"File Fields","text":"<p>File fields (including <code>ImageField</code>) are fully supported, including tracking changes via <code>.save()</code> and <code>.delete()</code>:</p> <pre><code>class Document(DirtyFieldsMixin, models.Model):\n    file = models.FileField(upload_to='docs/')\n\n&gt;&gt;&gt; doc = Document.objects.get(pk=1)\n&gt;&gt;&gt; doc.file.save('new.txt', content)  # Tracked!\n&gt;&gt;&gt; doc.is_dirty()\nTrue\n\n&gt;&gt;&gt; doc.file.delete()  # Also tracked!\n&gt;&gt;&gt; doc.is_dirty()\nTrue\n</code></pre>"},{"location":"guide/basic-usage/#f-expressions","title":"F() Expressions","text":"<p>Fields assigned with an <code>F()</code> expression (or any other ORM expression like <code>Concat</code>, <code>Func</code>, \u2026) are not reported as dirty. The expression is a directive the ORM resolves at save time, not a value change you can compare against the saved value.</p> <pre><code>from django.db.models import F\n\n&gt;&gt;&gt; obj.count\n5\n&gt;&gt;&gt; obj.count = F('count') + 1\n&gt;&gt;&gt; obj.is_dirty()\nFalse\n&gt;&gt;&gt; obj.get_dirty_fields()\n{}\n</code></pre> <p>On Django 6.0+, <code>Model.save()</code> auto-refreshes fields assigned to expressions, so after <code>obj.save()</code> the field holds its resolved value (e.g. <code>6</code>) and tracking resumes as normal:</p> <pre><code>&gt;&gt;&gt; obj.save()\n&gt;&gt;&gt; obj.count\n6\n&gt;&gt;&gt; obj.count = 10\n&gt;&gt;&gt; obj.get_dirty_fields()\n{'count': 6}\n</code></pre> <p>If you <code>save(update_fields={...})</code> and the expression-bearing field isn't included, the auto-refresh skips it \u2014 the field keeps holding the unresolved expression until a later full <code>save()</code> or <code>refresh_from_db()</code>.</p>"},{"location":"guide/basic-usage/#new-unsaved-models","title":"New (Unsaved) Models","text":"<p>New model instances that haven't been saved are always considered dirty:</p> <pre><code>&gt;&gt;&gt; obj = MyModel(name=\"test\")\n&gt;&gt;&gt; obj.is_dirty()\nTrue\n&gt;&gt;&gt; obj.save()\n&gt;&gt;&gt; obj.is_dirty()\nFalse\n</code></pre>"},{"location":"reference/api/","title":"API Reference","text":""},{"location":"reference/api/#dirtyfieldsmixin","title":"DirtyFieldsMixin","text":"<p>The main mixin class to add dirty field tracking to Django models.</p> <pre><code>from dirtyfields import DirtyFieldsMixin\n\nclass MyModel(DirtyFieldsMixin, models.Model):\n    ...\n</code></pre>"},{"location":"reference/api/#class-attributes","title":"Class Attributes","text":""},{"location":"reference/api/#fields_to_check","title":"<code>FIELDS_TO_CHECK</code>","text":"<p>Optional list of field names to track (whitelist). If not set, all fields are tracked.</p> <pre><code>class MyModel(DirtyFieldsMixin, models.Model):\n    FIELDS_TO_CHECK = ['name', 'status']\n\n    name = models.CharField(max_length=100)\n    status = models.CharField(max_length=20)\n    description = models.TextField()  # Not tracked\n</code></pre> <p>Type: <code>list[str] | None</code></p> <p>Default: <code>None</code> (track all fields)</p> <p>Mutual Exclusion</p> <p>Cannot be used together with <code>FIELDS_TO_CHECK_EXCLUDE</code>. Using both will raise a <code>ValueError</code>.</p>"},{"location":"reference/api/#fields_to_check_exclude","title":"<code>FIELDS_TO_CHECK_EXCLUDE</code>","text":"<p>Optional list of field names to exclude from tracking (blacklist). All other fields are tracked.</p> <pre><code>class MyModel(DirtyFieldsMixin, models.Model):\n    FIELDS_TO_CHECK_EXCLUDE = ['updated_at', 'last_login']\n\n    name = models.CharField(max_length=100)\n    email = models.EmailField()\n    updated_at = models.DateTimeField(auto_now=True)  # Not tracked\n    last_login = models.DateTimeField(null=True)  # Not tracked\n</code></pre> <p>Type: <code>list[str] | None</code></p> <p>Default: <code>None</code> (track all fields)</p> <p>Mutual Exclusion</p> <p>Cannot be used together with <code>FIELDS_TO_CHECK</code>. Using both will raise a <code>ValueError</code>.</p>"},{"location":"reference/api/#enable_m2m_check","title":"<code>ENABLE_M2M_CHECK</code>","text":"<p>Enable Many-to-Many field tracking. When enabled, M2M fields can be checked using the <code>check_m2m</code> parameter.</p> <pre><code>class MyModel(DirtyFieldsMixin, models.Model):\n    ENABLE_M2M_CHECK = True\n\n    tags = models.ManyToManyField(Tag)\n</code></pre> <p>Type: <code>bool</code></p> <p>Default: <code>False</code></p>"},{"location":"reference/api/#track_mutations","title":"<code>TRACK_MUTATIONS</code>","text":"<p>Detect in-place mutations of mutable field values (e.g. <code>obj.json_field[\"k\"] = \"v\"</code> or <code>obj.tags_list.append(\"new\")</code>). Off by default: the descriptor-based approach only tracks assignments via <code>__set__</code>, so mutations through a live reference go unnoticed. Enabling this snapshots mutable values (<code>dict</code>, <code>list</code>, <code>set</code>, <code>bytearray</code>) via <code>deepcopy</code> on first read, so <code>get_dirty_fields()</code> can compare and detect changes.</p> <pre><code>class MyModel(DirtyFieldsMixin, models.Model):\n    TRACK_MUTATIONS = True\n\n    data = models.JSONField(default=dict)\n</code></pre> <p>Type: <code>bool</code></p> <p>Default: <code>False</code></p> <p>Cost</p> <p>A <code>deepcopy</code> per mutable-valued field on first read. Leave off when you only reassign fields \u2014 the default is correct and free for the common case.</p>"},{"location":"reference/api/#compare_function","title":"<code>compare_function</code>","text":"<p>Custom comparison function for determining if field values have changed. Useful for timezone-aware datetime comparisons.</p> <pre><code>from dirtyfields import DirtyFieldsMixin, timezone_support_compare\n\nclass MyModel(DirtyFieldsMixin, models.Model):\n    compare_function = (timezone_support_compare, {})\n\n    updated_at = models.DateTimeField()\n</code></pre> <p>Type: <code>tuple[Callable[..., bool], dict[str, Any]] | None</code></p> <p>Default: <code>None</code> (uses simple equality)</p>"},{"location":"reference/api/#normalise_function","title":"<code>normalise_function</code>","text":"<p>Custom function to transform values before returning them from <code>get_dirty_fields()</code>.</p> <pre><code>from dirtyfields import DirtyFieldsMixin, normalise_value\n\nclass MyModel(DirtyFieldsMixin, models.Model):\n    normalise_function = (normalise_value, {})\n</code></pre> <p>Type: <code>tuple[Callable[..., Any], dict[str, Any]] | None</code></p> <p>Default: <code>None</code> (returns values unchanged)</p>"},{"location":"reference/api/#methods","title":"Methods","text":""},{"location":"reference/api/#is_dirtycheck_relationshipfalse-check_m2mfalse","title":"<code>is_dirty(check_relationship=False, check_m2m=False)</code>","text":"<p>Check if the model instance has any unsaved changes.</p> <p>Parameters:</p> Parameter Type Default Description <code>check_relationship</code> <code>bool</code> <code>False</code> Include foreign key fields in the check <code>check_m2m</code> <code>bool</code> <code>False</code> Include M2M fields in the check (requires <code>ENABLE_M2M_CHECK=True</code>) <p>Returns: <code>bool</code> - <code>True</code> if any tracked fields have changed</p> <p>Raises: <code>ValueError</code> if <code>check_m2m=True</code> but <code>ENABLE_M2M_CHECK</code> is <code>False</code></p> <p>Example:</p> <pre><code>&gt;&gt;&gt; obj = MyModel.objects.get(pk=1)\n&gt;&gt;&gt; obj.is_dirty()\nFalse\n&gt;&gt;&gt; obj.name = \"changed\"\n&gt;&gt;&gt; obj.is_dirty()\nTrue\n\n# M2M check (requires ENABLE_M2M_CHECK = True)\n&gt;&gt;&gt; obj.is_dirty(check_m2m=True)  # Captures original state on first call\nFalse\n&gt;&gt;&gt; obj.tags.add(new_tag)\n&gt;&gt;&gt; obj.is_dirty(check_m2m=True)\nTrue\n</code></pre>"},{"location":"reference/api/#get_dirty_fieldscheck_relationshipfalse-check_m2mfalse-verbosefalse","title":"<code>get_dirty_fields(check_relationship=False, check_m2m=False, verbose=False)</code>","text":"<p>Get a dictionary of fields that have been modified.</p> <p>Parameters:</p> Parameter Type Default Description <code>check_relationship</code> <code>bool</code> <code>False</code> Include foreign key fields <code>check_m2m</code> <code>bool</code> <code>False</code> Include M2M fields (requires <code>ENABLE_M2M_CHECK=True</code>) <code>verbose</code> <code>bool</code> <code>False</code> Return both old and new values <p>Returns: <code>dict</code> - Dictionary mapping field names to original values (or to <code>{'saved': old, 'current': new}</code> if verbose)</p> <p>Raises: <code>ValueError</code> if <code>check_m2m=True</code> but <code>ENABLE_M2M_CHECK</code> is <code>False</code></p> <p>Example:</p> <pre><code>&gt;&gt;&gt; obj.name = \"new\"\n&gt;&gt;&gt; obj.get_dirty_fields()\n{'name': 'old'}\n\n&gt;&gt;&gt; obj.get_dirty_fields(verbose=True)\n{'name': {'saved': 'old', 'current': 'new'}}\n\n# M2M check (requires ENABLE_M2M_CHECK = True)\n&gt;&gt;&gt; obj.get_dirty_fields(check_m2m=True)  # Captures original state on first call\n{}\n&gt;&gt;&gt; obj.tags.add(new_tag)\n&gt;&gt;&gt; obj.get_dirty_fields(check_m2m=True)\n{'tags': {1, 2}}  # Returns original state before changes\n</code></pre>"},{"location":"reference/api/#was_dirtycheck_relationshipfalse-check_m2mfalse","title":"<code>was_dirty(check_relationship=False, check_m2m=False)</code>","text":"<p>Check if instance was dirty before the last save.</p> <p>Parameters:</p> Parameter Type Default Description <code>check_relationship</code> <code>bool</code> <code>False</code> Include foreign key fields <code>check_m2m</code> <code>bool</code> <code>False</code> Include M2M fields (requires <code>ENABLE_M2M_CHECK=True</code>) <p>Returns: <code>bool</code> - <code>True</code> if any tracked fields were dirty before the last save</p> <p>Raises: <code>ValueError</code> if <code>check_m2m=True</code> but <code>ENABLE_M2M_CHECK</code> is <code>False</code></p> <p>Example:</p> <pre><code>&gt;&gt;&gt; obj.name = \"new\"\n&gt;&gt;&gt; obj.save()\n&gt;&gt;&gt; obj.was_dirty()\nTrue\n\n# M2M check (requires ENABLE_M2M_CHECK = True)\n&gt;&gt;&gt; obj.tags.add(new_tag)\n&gt;&gt;&gt; obj.save()\n&gt;&gt;&gt; obj.was_dirty(check_m2m=True)\nTrue\n</code></pre>"},{"location":"reference/api/#get_was_dirty_fieldscheck_relationshipfalse-check_m2mfalse","title":"<code>get_was_dirty_fields(check_relationship=False, check_m2m=False)</code>","text":"<p>Get fields that were dirty before the last save.</p> <p>Parameters:</p> Parameter Type Default Description <code>check_relationship</code> <code>bool</code> <code>False</code> Include foreign key fields <code>check_m2m</code> <code>bool</code> <code>False</code> Include M2M fields (requires <code>ENABLE_M2M_CHECK=True</code>) <p>Returns: <code>dict</code> - Dictionary mapping field names to original values from before the last save</p> <p>Raises: <code>ValueError</code> if <code>check_m2m=True</code> but <code>ENABLE_M2M_CHECK</code> is <code>False</code></p> <p>Example:</p> <pre><code>&gt;&gt;&gt; obj.name = \"new\"\n&gt;&gt;&gt; obj.save()\n&gt;&gt;&gt; obj.get_was_dirty_fields()\n{'name': 'old'}\n\n# M2M check (requires ENABLE_M2M_CHECK = True)\n&gt;&gt;&gt; obj.tags.add(new_tag)\n&gt;&gt;&gt; obj.save()\n&gt;&gt;&gt; obj.get_was_dirty_fields(check_m2m=True)\n{'tags': {1, 2}}  # Original M2M state before changes\n</code></pre>"},{"location":"reference/api/#save_dirty_fields","title":"<code>save_dirty_fields()</code>","text":"<p>Save only the fields that have been modified.</p> <p>Parameters: None</p> <p>Returns: None</p> <p>Raises: <code>ValueError</code> if the model has never been saved (no primary key)</p> <p>Example:</p> <pre><code>&gt;&gt;&gt; obj.name = \"changed\"\n&gt;&gt;&gt; obj.save_dirty_fields()  # Only updates 'name' column\n</code></pre>"},{"location":"reference/api/#asaveargs-kwargs-async","title":"<code>asave(*args, **kwargs)</code> (async)","text":"<p>Async equivalent of <code>Model.save()</code> with dirty tracking. Captures dirty state into <code>_was_dirty_fields</code>, calls <code>super().asave()</code>, then resets the dirty state.</p> <p>Example:</p> <pre><code>obj.name = \"changed\"\nawait obj.asave()\nobj.is_dirty()      # False\nobj.was_dirty()     # True\n</code></pre>"},{"location":"reference/api/#refresh_from_dbusingnone-fieldsnone-from_querysetnone","title":"<code>refresh_from_db(using=None, fields=None, from_queryset=None)</code>","text":"<p>Override of <code>Model.refresh_from_db()</code> that also resets the dirty state. If <code>fields</code> is provided, only those fields have their dirty state reset.</p>"},{"location":"reference/api/#arefresh_from_dbusingnone-fieldsnone-from_querysetnone-async","title":"<code>arefresh_from_db(using=None, fields=None, from_queryset=None)</code> (async)","text":"<p>Async equivalent of <code>refresh_from_db()</code>.</p>"},{"location":"reference/api/#utility-functions","title":"Utility Functions","text":""},{"location":"reference/api/#raw_comparenew_value-old_value","title":"<code>raw_compare(new_value, old_value)</code>","text":"<p>Default comparison function using simple equality.</p> <pre><code>&gt;&gt;&gt; from dirtyfields import raw_compare\n&gt;&gt;&gt; raw_compare(\"a\", \"a\")\nTrue\n&gt;&gt;&gt; raw_compare(\"a\", \"b\")\nFalse\n</code></pre>"},{"location":"reference/api/#normalise_valuevalue","title":"<code>normalise_value(value)</code>","text":"<p>Default normalisation function that returns the value unchanged.</p> <pre><code>&gt;&gt;&gt; from dirtyfields import normalise_value\n&gt;&gt;&gt; normalise_value({\"key\": \"value\"})\n{'key': 'value'}\n</code></pre>"},{"location":"reference/api/#timezone_support_comparenew_value-old_value-timezone_to_setutc","title":"<code>timezone_support_compare(new_value, old_value, timezone_to_set=UTC)</code>","text":"<p>Comparison function with timezone awareness handling for datetime values.</p> <p>When comparing datetime values where one is timezone-aware and the other is naive, this function converts as needed and emits a warning.</p> <p>Parameters:</p> Parameter Type Default Description <code>new_value</code> <code>Any</code> - The new (current) value <code>old_value</code> <code>Any</code> - The old (saved) value <code>timezone_to_set</code> <code>tzinfo</code> <code>UTC</code> Timezone to use when converting naive datetimes <p>Returns: <code>bool</code> - <code>True</code> if values are equal</p> <p>Example:</p> <pre><code>from dirtyfields import DirtyFieldsMixin, timezone_support_compare\n\nclass MyModel(DirtyFieldsMixin, models.Model):\n    compare_function = (timezone_support_compare, {})\n    updated_at = models.DateTimeField()\n</code></pre>"},{"location":"reference/api/#bulk-operation-helpers","title":"Bulk Operation Helpers","text":"<p>These functions help track dirty state when using bulk operations like <code>bulk_update()</code>, which bypass the model's <code>save()</code> method.</p>"},{"location":"reference/api/#capture_dirty_stateinstances","title":"<code>capture_dirty_state(instances)</code>","text":"<p>Capture current dirty state for multiple instances before a bulk operation.</p> <p>Call this before <code>bulk_update()</code> to preserve the dirty state for later inspection via <code>was_dirty()</code> / <code>get_was_dirty_fields()</code>.</p> <p>Parameters:</p> Parameter Type Description <code>instances</code> <code>Iterable[DirtyFieldsMixin]</code> Model instances to capture state for <p>Returns: <code>None</code></p>"},{"location":"reference/api/#reset_dirty_stateinstances-fieldsnone","title":"<code>reset_dirty_state(instances, fields=None)</code>","text":"<p>Reset dirty tracking state for multiple instances after a bulk operation.</p> <p>Call this after <code>bulk_update()</code> to clear the dirty state, indicating that changes have been persisted.</p> <p>Parameters:</p> Parameter Type Default Description <code>instances</code> <code>Iterable[DirtyFieldsMixin]</code> - Model instances to reset state for <code>fields</code> <code>Iterable[str] \\| None</code> <code>None</code> If provided, only reset these specific fields. Otherwise reset all. <p>Returns: <code>None</code></p> <p>Example:</p> <pre><code>from dirtyfields import capture_dirty_state, reset_dirty_state\n\n# Modify instances\ninstances = list(MyModel.objects.filter(status='pending'))\nfor obj in instances:\n    obj.status = 'processed'\n\n# Capture state before bulk update\ncapture_dirty_state(instances)\n\n# Perform bulk update\nMyModel.objects.bulk_update(instances, ['status'])\n\n# Reset state after bulk update\nreset_dirty_state(instances)\n\n# Now you can check what was changed\nfor obj in instances:\n    if obj.was_dirty():\n        print(f\"Object {obj.pk} had changes: {obj.get_was_dirty_fields()}\")\n</code></pre>"},{"location":"reference/api/#module-info","title":"Module Info","text":""},{"location":"reference/api/#__version__","title":"<code>__version__</code>","text":"<p>The package version string. Read at import time from installed package metadata via <code>importlib.metadata.version(\"django-filthyfields\")</code>, so it always reflects the version pip/uv resolved to.</p> <pre><code>&gt;&gt;&gt; from dirtyfields import __version__\n&gt;&gt;&gt; __version__  # e.g. '1.9.8b6'\n</code></pre>"},{"location":"reference/changelog/","title":"Changelog","text":"<p>All notable changes to this project will be documented in this file.</p> <p>The format is based on Keep a Changelog, and this project adheres to Semantic Versioning.</p>"},{"location":"reference/changelog/#198b8-2026-04-25","title":"[1.9.8b8] - 2026-04-25","text":""},{"location":"reference/changelog/#added","title":"Added","text":"<ul> <li><code>TRACK_MUTATIONS</code> opt-in: detects in-place mutations of mutable field values (<code>dict</code>, <code>list</code>, <code>set</code>, <code>bytearray</code>).</li> </ul>"},{"location":"reference/changelog/#fixed","title":"Fixed","text":"<ul> <li><code>F()</code> and other ORM expressions are no longer reported as dirty \u2014 they're directives the ORM resolves at save time, not value changes.</li> </ul>"},{"location":"reference/changelog/#198b7-2026-04-24","title":"[1.9.8b7] - 2026-04-24","text":""},{"location":"reference/changelog/#added_1","title":"Added","text":"<ul> <li>Compiled wheels (<code>cp314</code>, <code>cp314t</code>); ~50% less <code>__get__</code> overhead on the compiled path.</li> <li>Free-threaded Python 3.14t support.</li> </ul>"},{"location":"reference/changelog/#changed","title":"Changed","text":"<ul> <li>Build backend: <code>hatchling</code> \u2192 <code>setuptools</code>.</li> <li>File-field tracking now uses an <code>attr_class</code> subclass instead of monkey-patching <code>FieldFile.save</code> / <code>.delete</code>.</li> </ul>"},{"location":"reference/changelog/#198b5-2026-04-23","title":"[1.9.8b5] - 2026-04-23","text":""},{"location":"reference/changelog/#changed_1","title":"Changed","text":"<ul> <li>Dropped support for Python 3.11, 3.12, 3.13; Python 3.14+ required</li> <li>Dropped support for Django 4.2 and 5.2; Django 6.0+ required</li> <li>Development status bumped to Beta</li> <li>Bumped dev dependencies (mypy, ruff, ty, pytest-django, pytest-cov, pre-commit, mkdocs-material, mike, hatchling) and <code>django-stubs</code> 5.2.8 \u2192 6.0.3</li> </ul>"},{"location":"reference/changelog/#fixed_1","title":"Fixed","text":"<ul> <li>Documentation: <code>check_m2m</code> parameter on <code>is_dirty()</code> and <code>get_dirty_fields()</code> is a <code>bool</code>, not a dict</li> <li>Documentation: benchmark invocation path</li> <li>Documentation: <code>__version__</code> example now reflects actual version format</li> <li>Documentation: added async methods (<code>asave()</code>, <code>arefresh_from_db()</code>) and bulk helpers to the guide and API reference</li> </ul>"},{"location":"reference/changelog/#removed","title":"Removed","text":"<ul> <li><code>SECURITY.md</code> and <code>.github/ISSUE_TEMPLATE/</code> (GitHub issues disabled on this fork)</li> </ul>"},{"location":"reference/changelog/#198b4-2026-04-06","title":"[1.9.8b4] - 2026-04-06","text":""},{"location":"reference/changelog/#added_2","title":"Added","text":"<ul> <li>Async support: <code>asave()</code> and <code>arefresh_from_db()</code> overrides for dirty-tracking in async code paths</li> <li><code>FIELDS_TO_CHECK_EXCLUDE</code> class attribute for blacklist-style field filtering (alternative to <code>FIELDS_TO_CHECK</code>)</li> <li><code>capture_dirty_state()</code> and <code>reset_dirty_state()</code> helper functions for bulk operations (<code>bulk_update()</code> and similar)</li> </ul>"},{"location":"reference/changelog/#changed_2","title":"Changed","text":"<ul> <li>Tests synced with upstream django-dirtyfields v1.9.9 (F() expression auto-refresh behavior on Django 6.0, deterministic timezone tests)</li> </ul>"},{"location":"reference/changelog/#198b3-2026-01-22","title":"[1.9.8b3] - 2026-01-22","text":""},{"location":"reference/changelog/#changed_3","title":"Changed","text":"<ul> <li>Performance: <code>__get__</code> uses try/except for faster dict access</li> <li>Performance: <code>__set__</code> has early returns and caches <code>FIELDS_TO_CHECK</code> per instance</li> <li>Performance: <code>_values_equal</code> has a fast path for same-type comparisons</li> <li>Performance: <code>_normalize_value</code> uses shallow copy for simple dicts/lists (common for JSONField)</li> <li>Benchmarks diversified across DateTime, Decimal, JSON, Float, and Text fields</li> </ul>"},{"location":"reference/changelog/#198b2-2026-01-17","title":"[1.9.8b2] - 2026-01-17","text":""},{"location":"reference/changelog/#added_3","title":"Added","text":"<ul> <li>Django 4.2 LTS, 5.2, and 6.0 support</li> <li>Python 3.11 and 3.12 support</li> <li>Python 3.14t (free-threaded) support</li> </ul>"},{"location":"reference/changelog/#changed_4","title":"Changed","text":"<ul> <li>Minimum Python version is now 3.11 (from 3.13)</li> <li>Minimum Django version is now 4.2 LTS (from 5.1)</li> </ul>"},{"location":"reference/changelog/#198b1-2025-01-17","title":"[1.9.8b1] - 2025-01-17","text":""},{"location":"reference/changelog/#added_4","title":"Added","text":"<ul> <li>Re-added M2M field tracking with <code>ENABLE_M2M_CHECK</code> class attribute</li> <li>Re-added <code>check_m2m</code> parameter to <code>is_dirty()</code> and <code>get_dirty_fields()</code></li> <li>Re-added custom <code>compare_function</code> support for field value comparison</li> <li>Re-added custom <code>normalise_function</code> support for output value transformation</li> <li>Added <code>was_dirty()</code> and <code>get_was_dirty_fields()</code> methods</li> <li>Added <code>raw_compare</code>, <code>normalise_value</code>, and <code>timezone_support_compare</code> utility functions</li> </ul>"},{"location":"reference/changelog/#changed_5","title":"Changed","text":"<ul> <li>Version numbering now follows upstream django-dirtyfields for API compatibility indication</li> </ul>"},{"location":"reference/changelog/#010a2-2025-01-16","title":"[0.1.0a2] - 2025-01-16","text":""},{"location":"reference/changelog/#changed_6","title":"Changed","text":"<ul> <li>Test automated PyPI publishing pipeline</li> </ul>"},{"location":"reference/changelog/#010a1-2025-01-16","title":"[0.1.0a1] - 2025-01-16","text":""},{"location":"reference/changelog/#added_5","title":"Added","text":"<ul> <li>Initial release as <code>django-filthyfields</code> (fork of <code>django-dirtyfields</code>)</li> <li>Descriptor-based dirty field tracking (replaces signal-based approach)</li> <li>Support for <code>FIELDS_TO_CHECK</code> to limit tracked fields</li> <li>File field tracking including <code>.save()</code> and <code>.delete()</code> methods</li> <li>F() expression support</li> <li>Modern Python 3.13+ and Django 5.0+ support</li> </ul>"},{"location":"reference/changelog/#changed_7","title":"Changed","text":"<ul> <li>Complete rewrite using descriptors instead of <code>post_init</code>/<code>post_save</code> signals</li> <li>Significantly improved performance for model loading</li> </ul>"},{"location":"reference/changelog/#removed_1","title":"Removed","text":"<ul> <li>Python &lt; 3.13 support</li> <li>Django &lt; 5.0 support</li> </ul>"},{"location":"reference/changelog/#historical-releases-from-upstream-django-dirtyfields","title":"Historical Releases (from upstream django-dirtyfields)","text":""},{"location":"reference/changelog/#198-2025-11-17","title":"[1.9.8] - 2025-11-17","text":""},{"location":"reference/changelog/#added_6","title":"Added","text":"<ul> <li>Confirm support for Python 3.14</li> <li>Confirm support for Django 5.2</li> </ul>"},{"location":"reference/changelog/#removed_2","title":"Removed","text":"<ul> <li>Drop support for Python 3.9</li> <li>Drop support for Django 2.2, 3.0, 3.1</li> </ul>"},{"location":"reference/changelog/#197-2025-03-22","title":"[1.9.7] - 2025-03-22","text":""},{"location":"reference/changelog/#fixed_2","title":"Fixed","text":"<ul> <li>Fix <code>Model.refresh_from_db()</code> so calling it with 3 positional args works.</li> </ul>"},{"location":"reference/changelog/#196-2025-02-07","title":"[1.9.6] - 2025-02-07","text":""},{"location":"reference/changelog/#fixed_3","title":"Fixed","text":"<ul> <li>Allow passing <code>from_queryset</code> argument to <code>Model.refresh_from_db()</code> in Django 5.1+</li> </ul>"},{"location":"reference/changelog/#195-2024-11-09","title":"[1.9.5] - 2024-11-09","text":""},{"location":"reference/changelog/#fixed_4","title":"Fixed","text":"<ul> <li>Fixed error in PyPI publish process</li> </ul>"},{"location":"reference/changelog/#194-2024-11-09","title":"[1.9.4] - 2024-11-09","text":""},{"location":"reference/changelog/#added_7","title":"Added","text":"<ul> <li>Confirm support for Python 3.13</li> <li>Confirm support for Django 5.1</li> </ul>"},{"location":"reference/changelog/#removed_3","title":"Removed","text":"<ul> <li>Drop support for Python 3.8</li> </ul>"},{"location":"reference/changelog/#193-2024-05-24","title":"[1.9.3] - 2024-05-24","text":""},{"location":"reference/changelog/#added_8","title":"Added","text":"<ul> <li>Confirm support for Python 3.12</li> <li>Confirm support for Django 5.0</li> </ul>"},{"location":"reference/changelog/#removed_4","title":"Removed","text":"<ul> <li>Drop support for Python 3.7</li> <li>Drop support for Django 2.0, 2.1</li> </ul>"},{"location":"reference/changelog/#192-2023-04-12","title":"[1.9.2] - 2023-04-12","text":""},{"location":"reference/changelog/#added_9","title":"Added","text":"<ul> <li>Confirm support for Django 4.2</li> </ul>"},{"location":"reference/changelog/#191-2023-01-14","title":"[1.9.1] - 2023-01-14","text":""},{"location":"reference/changelog/#fixed_5","title":"Fixed","text":"<ul> <li>Fixed a <code>KeyError</code> that would occur when updating a field two times in a row when the field value is set to an <code>F</code> object and the field is specified in the <code>update_fields</code> argument to <code>save()</code>. (#209)</li> </ul>"},{"location":"reference/changelog/#190-2022-11-07","title":"[1.9.0] - 2022-11-07","text":""},{"location":"reference/changelog/#added_10","title":"Added","text":"<ul> <li>Confirm support for Python 3.11</li> <li>Confirm support for Django 4.1</li> </ul>"},{"location":"reference/changelog/#changed_8","title":"Changed","text":"<ul> <li>The method <code>get_dirty_fields()</code> now returns only the file name for FileFields. This is to improve performance, since the entire <code>FieldFile</code> object will no longer be copied when Model instances are initialized and saved. (#203)</li> </ul>"},{"location":"reference/changelog/#fixed_6","title":"Fixed","text":"<ul> <li>The method <code>save_dirty_fields()</code> can now be called on Model instances that have not been saved to the Database yet. In this case all fields will be considered dirty, and all will be saved to the Database. Previously doing this would result in an Exception being raised. (#200)</li> </ul>"},{"location":"reference/changelog/#removed_5","title":"Removed","text":"<ul> <li>Drop support for Django 1.11</li> </ul>"},{"location":"reference/changelog/#182-2022-07-16","title":"[1.8.2] - 2022-07-16","text":""},{"location":"reference/changelog/#documentation","title":"Documentation","text":"<ul> <li>General improvements to content and generation of Documentation (#197).</li> </ul>"},{"location":"reference/changelog/#181-2022-03-07","title":"[1.8.1] - 2022-03-07","text":""},{"location":"reference/changelog/#documentation_1","title":"Documentation","text":"<ul> <li>Document limitations when using dirtyfields and database transactions (#148).</li> <li>Document how to use a Proxy Model to avoid performance impact (#132).</li> </ul>"},{"location":"reference/changelog/#180-2022-01-22","title":"[1.8.0] - 2022-01-22","text":""},{"location":"reference/changelog/#added_11","title":"Added","text":"<ul> <li>Confirm support of Python 3.10</li> <li>Confirm support of Django 4.0</li> </ul>"},{"location":"reference/changelog/#changed_9","title":"Changed","text":"<ul> <li>Run CI tests on Github Actions since travis-ci.org has been shutdown.</li> </ul>"},{"location":"reference/changelog/#removed_6","title":"Removed","text":"<ul> <li>Drop support for Python 3.6</li> </ul>"},{"location":"reference/changelog/#170-2021-05-02","title":"[1.7.0] - 2021-05-02","text":""},{"location":"reference/changelog/#added_12","title":"Added","text":"<ul> <li>Provide programmatically accessible package version number. Use <code>dirtyfields.__version__</code> for a string, <code>dirtyfields.VERSION</code> for a tuple.</li> <li>Build and publish a wheel to PyPI.</li> </ul>"},{"location":"reference/changelog/#changed_10","title":"Changed","text":"<ul> <li>Only look at concrete fields when determining dirty fields.</li> <li>Migrate package metadata from setup.py to setup.cfg and specify the PEP-517 build-backend to use with the project.</li> </ul>"},{"location":"reference/changelog/#fixed_7","title":"Fixed","text":"<ul> <li>Fixed a <code>KeyError</code> that happened when saving a Model with <code>update_fields</code> specified after updating a field value with an <code>F</code> object (#118).</li> </ul>"},{"location":"reference/changelog/#160-2021-04-07","title":"[1.6.0] - 2021-04-07","text":""},{"location":"reference/changelog/#added_13","title":"Added","text":"<ul> <li>Confirm support of Django 3.2</li> </ul>"},{"location":"reference/changelog/#changed_11","title":"Changed","text":"<ul> <li>Remove pytz as a dependency.</li> </ul>"},{"location":"reference/changelog/#150-2021-01-15","title":"[1.5.0] - 2021-01-15","text":""},{"location":"reference/changelog/#added_14","title":"Added","text":"<ul> <li>Confirm support of Python 3.8</li> <li>Confirm support of Python 3.9</li> <li>Confirm support of Django 3.0</li> <li>Confirm support of Django 3.1</li> </ul>"},{"location":"reference/changelog/#removed_7","title":"Removed","text":"<ul> <li>Drop support of Python 2.7</li> <li>Drop support of Python 3.5</li> </ul>"},{"location":"reference/changelog/#141-2020-11-28","title":"[1.4.1] - 2020-11-28","text":""},{"location":"reference/changelog/#fixed_8","title":"Fixed","text":"<ul> <li>Fixes an issue when <code>refresh_from_db</code> was called with the <code>fields</code> argument, the dirty state for all fields would be reset, even though only the fields specified are reloaded from the database. Now only the reloaded fields will have their dirty state reset (#154).</li> <li>Fixes an issue where accessing a deferred field would reset the dirty state for all fields (#154).</li> </ul>"},{"location":"reference/changelog/#14-2020-04-11","title":"[1.4] - 2020-04-11","text":""},{"location":"reference/changelog/#added_15","title":"Added","text":"<ul> <li>Confirm support of Python 3.7</li> <li>Confirm support of Django 2.0, 2.1, 2.2</li> </ul>"},{"location":"reference/changelog/#fixed_9","title":"Fixed","text":"<ul> <li>Fixes tests for Django 2.0</li> <li><code>refresh_from_db</code> is now properly resetting dirty fields.</li> <li>Adds <code>normalise_function</code> to provide control on how dirty values are stored</li> </ul>"},{"location":"reference/changelog/#removed_8","title":"Removed","text":"<ul> <li>Drop support of Python 3.4</li> <li>Drop support of Django 1.8, 1.9, 1.10</li> </ul>"},{"location":"reference/changelog/#131-2018-02-28","title":"[1.3.1] - 2018-02-28","text":""},{"location":"reference/changelog/#added_16","title":"Added","text":"<ul> <li>Updates python classifier in setup file (#116). Thanks amureki.</li> <li>Adds PEP8 validation in travisCI run (#123). Thanks hsmett.</li> </ul>"},{"location":"reference/changelog/#fixed_10","title":"Fixed","text":"<ul> <li>Avoids <code>get_deferred_fields</code> to be called too many times on <code>_as_dict</code> (#115). Thanks benjaminrigaud.</li> <li>Respects <code>FIELDS_TO_CHECK</code> in <code>reset_state</code> (#114). Thanks bparker98.</li> </ul>"},{"location":"reference/changelog/#13-2017-08-23","title":"[1.3] - 2017-08-23","text":""},{"location":"reference/changelog/#added_17","title":"Added","text":"<ul> <li>Add test coverage for Django 1.11.</li> <li>A new attribute <code>FIELDS_TO_CHECK</code> has been added to <code>DirtyFieldsMixin</code> to specify a limited set of fields to check.</li> </ul>"},{"location":"reference/changelog/#fixed_11","title":"Fixed","text":"<ul> <li>Fixes issue with verbose mode when the object has not been yet saved in the database (MR #99). Thanks vapkarian.</li> <li>Correctly handle <code>ForeignKey.db_column</code> <code>{}_id</code> in <code>update_fields</code>. Thanks Hugo Smett.</li> <li>Fixes #111: Eliminate a memory leak.</li> <li>Handle deferred fields in <code>update_fields</code></li> </ul>"},{"location":"reference/changelog/#removed_9","title":"Removed","text":"<ul> <li>Drop support for unsupported Django versions: 1.4, 1.5, 1.6 and 1.7 series.</li> </ul>"},{"location":"reference/changelog/#121-2016-11-16","title":"[1.2.1] - 2016-11-16","text":""},{"location":"reference/changelog/#added_18","title":"Added","text":"<ul> <li><code>django-dirtyfields</code> is now tested with PostgreSQL, especially with specific fields</li> </ul>"},{"location":"reference/changelog/#fixed_12","title":"Fixed","text":"<ul> <li>Fixes #80: Use of <code>Field.rel</code> raises warnings from Django 1.9+</li> <li>Fixes #84: Use <code>only()</code> in conjunction with 2 foreign keys triggers a recursion error</li> <li>Fixes #77: Shallow copy does not work with Django 1.9's JSONField</li> <li>Fixes #88: <code>get_dirty_fields</code> on a newly-created model does not work if pk is specified</li> <li>Fixes #90: Unmark dirty fields only listed in <code>update_fields</code></li> </ul>"},{"location":"reference/changelog/#12-2016-08-11","title":"[1.2] - 2016-08-11","text":""},{"location":"reference/changelog/#added_19","title":"Added","text":"<ul> <li><code>django-dirtyfields</code> is now compatible with Django 1.10 series (deferred field handling has been updated).</li> </ul>"},{"location":"reference/changelog/#11-2016-08-04","title":"[1.1] - 2016-08-04","text":""},{"location":"reference/changelog/#added_20","title":"Added","text":"<ul> <li>A new attribute <code>ENABLE_M2M_CHECK</code> has been added to <code>DirtyFieldsMixin</code> to enable/disable m2m check functionality. This parameter is set to <code>False</code> by default.</li> </ul> <p>Breaking Change</p> <p>Backward incompatibility with v1.0.x series. If you were using <code>check_m2m</code> parameter to check m2m relations, you should now add <code>ENABLE_M2M_CHECK = True</code> to these models inheriting from <code>DirtyFieldsMixin</code>.</p>"},{"location":"reference/changelog/#101-2016-07-25","title":"[1.0.1] - 2016-07-25","text":""},{"location":"reference/changelog/#fixed_13","title":"Fixed","text":"<ul> <li>Fixing a bug preventing <code>django-dirtyfields</code> to work properly on models with custom primary keys.</li> </ul>"},{"location":"reference/changelog/#10-2016-06-26","title":"[1.0] - 2016-06-26","text":"<p>After several years of existence, django-dirty-fields is mature enough to switch to 1.X version.</p>"},{"location":"reference/changelog/#changed_12","title":"Changed","text":"<ul> <li><code>get_dirty_fields</code> is now more consistent for models not yet saved in the database. <code>get_dirty_fields</code> is, in that situation, always returning ALL fields, where it was before returning various results depending on how you initialised your model.</li> </ul> <p>Breaking Change</p> <p>It may affect you specially if you are using <code>get_dirty_fields</code> in a <code>pre_save</code> receiver. See more details at https://github.com/romgar/django-dirtyfields/issues/65.</p>"},{"location":"reference/changelog/#added_21","title":"Added","text":"<ul> <li>Adding compatibility for old _meta API, deprecated in Django 1.10 version and now replaced by an official API.</li> <li>General test cleaning.</li> </ul>"},{"location":"reference/changelog/#09-2016-06-18","title":"[0.9] - 2016-06-18","text":""},{"location":"reference/changelog/#added_22","title":"Added","text":"<ul> <li>Adding Many-to-Many fields comparison method <code>check_m2m</code> in <code>DirtyFieldsMixin</code>.</li> <li>Adding <code>verbose</code> parameter in <code>get_dirty_fields</code> method to get old AND new field values.</li> </ul>"},{"location":"reference/changelog/#082-2016-03-19","title":"[0.8.2] - 2016-03-19","text":""},{"location":"reference/changelog/#added_23","title":"Added","text":"<ul> <li>Adding field comparison method <code>compare_function</code> in <code>DirtyFieldsMixin</code>.</li> <li>Also adding a specific comparison function <code>timezone_support_compare</code> to handle different Datetime situations.</li> </ul>"},{"location":"reference/changelog/#081-2015-12-08","title":"[0.8.1] - 2015-12-08","text":""},{"location":"reference/changelog/#fixed_14","title":"Fixed","text":"<ul> <li>Not comparing fields that are deferred (<code>only</code> method on <code>QuerySet</code>).</li> <li>Being more tolerant when comparing values that can be on another type than expected.</li> </ul>"},{"location":"reference/changelog/#08-2015-10-30","title":"[0.8] - 2015-10-30","text":""},{"location":"reference/changelog/#added_24","title":"Added","text":"<ul> <li>Adding <code>save_dirty_fields</code> method to save only dirty fields in the database.</li> </ul>"},{"location":"reference/changelog/#07-2015-06-18","title":"[0.7] - 2015-06-18","text":""},{"location":"reference/changelog/#added_25","title":"Added","text":"<ul> <li>Using <code>copy</code> to properly track dirty fields on complex fields.</li> <li>Using <code>py.test</code> for tests launching.</li> </ul>"},{"location":"reference/changelog/#061-2015-06-14","title":"[0.6.1] - 2015-06-14","text":""},{"location":"reference/changelog/#fixed_15","title":"Fixed","text":"<ul> <li>Preventing django db expressions to be evaluated when testing dirty fields (#39).</li> </ul>"},{"location":"reference/changelog/#06-2015-06-11","title":"[0.6] - 2015-06-11","text":""},{"location":"reference/changelog/#added_26","title":"Added","text":"<ul> <li>Using <code>to_python</code> to avoid false positives when dealing with model fields that internally convert values (#4)</li> </ul>"},{"location":"reference/changelog/#fixed_16","title":"Fixed","text":"<ul> <li>Using <code>attname</code> instead of <code>name</code> on fields to avoid massive useless queries on ForeignKey fields (#34). For this kind of field, <code>get_dirty_fields()</code> is now returning instance id, instead of instance itself.</li> </ul>"},{"location":"reference/changelog/#05-2015-05-06","title":"[0.5] - 2015-05-06","text":""},{"location":"reference/changelog/#added_27","title":"Added","text":"<ul> <li>Adding code compatibility for python3</li> <li>Launching travis-ci tests on python3</li> <li>Using <code>tox</code> to launch tests on Django 1.5, 1.6, 1.7 and 1.8 versions</li> <li>Updating <code>runtests.py</code> test script to run properly on every Django version.</li> </ul>"},{"location":"reference/changelog/#fixed_17","title":"Fixed","text":"<ul> <li>Catching <code>Error</code> when trying to get foreign key object if not existing (#32).</li> </ul>"},{"location":"reference/changelog/#041-2015-04-08","title":"[0.4.1] - 2015-04-08","text":""},{"location":"reference/changelog/#fixed_18","title":"Fixed","text":"<ul> <li>Removing <code>model_to_form</code> to avoid bug when using models that have <code>editable=False</code> fields.</li> </ul>"},{"location":"reference/changelog/#04-2015-03-31","title":"[0.4] - 2015-03-31","text":""},{"location":"reference/changelog/#added_28","title":"Added","text":"<ul> <li>Adding <code>check_relationship</code> parameter on <code>is_dirty</code> and <code>get_dirty_field</code> methods to also check foreign key values.</li> </ul>"}]}